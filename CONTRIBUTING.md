Thanks for using vmware-fusion-py!

To contribute, just:
- Fork
- Open an Issue
- Pull Request

When adding new `vmrun` commands, verify the exact command name against `vmrun` with no arguments. For `vmcli` commands, check `vmcli <Module> <Command> --help`.

Thanks!
